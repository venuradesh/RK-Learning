import React from "react";
import styled from "styled-components";

const AddCourse = () => {
  return (
    <Container>
      <div>Adding a course</div>
    </Container>
  );
};

export default AddCourse;

const Container = styled.div``;
